{
  "name": "mneme",
  "private": true,
  "version": "0.1.0",
  "description": "Mneme \u2014 Personal AI Memory OS",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev:api": "pnpm --filter @mneme/api dev",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "eval": "pnpm --filter @mneme/evals test",
    "cli": "pnpm --filter @mneme/cli start",
    "mcp": "pnpm --filter @mneme/mcp-server start"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Danialsamadi/memory-os.git"
  }
}
