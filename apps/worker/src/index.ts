/**
 * Consolidation / conflict / decay worker — Week 3.
 * Stub keeps the process honest: idempotent job shape only.
 */
console.log(
  JSON.stringify({
    service: "mneme-worker",
    status: "stub",
    message:
      "Wire consolidate/conflict/decay jobs in Week 3 (see BUILD-PLAN.md).",
  }),
);
